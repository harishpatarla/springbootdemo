-- first migration file empty due to rebase on migration. This is for flyway to ignore any tables/triggers/objects pre-flyway integration.
-- flyway.baseline-on-migrate --> property to tell Whether to automatically call baseline when migrate is executed against a non-empty schema with no metadata table.
-- flyway.baseline-on-migrate=true is set in runtime properties for this to work for an existing schema


